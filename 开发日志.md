# 开发日志（课题六 · 行动克隆游戏智能体）

> 用途：按天凝练“做了什么、定了什么、留下什么”，方便后续直接引用、不必翻聊天记录。每天一条，详细依据见对应天的实践报告。

## 第 1 天（08-19）· 选题与需求

**目标**：定选题，写出立项书与需求规格。

**定了什么**

- 选题：课题六 Open P2P（腾讯 IEG），视觉-语言-动作（VLA）模型，看游戏画面直接输出键鼠动作。独立完成，周期 9 天。
- 必做（MVP）5 条：① 读懂 150M 模型输入/输出/训练方式；② 处理 200 条 toy 数据并出统计表；③ 评测达标：按键准确率≥55%、鼠标相关系数≥0.5；④ 同一测试片段模型输出 vs 人类标注对比（第 5 天录屏）；⑤ 代码 + 指标表 + 3000 字报告。
- 不做（明确排除）：`p2p-full-data` 全库、多模态扩展、生产部署、追 SOTA。
- 三条核心用户故事 + Given-When-Then 验收句（详见需求规格）。

**定下的约定**

- 技术栈 Python + PyTorch；算力用学校 NVIDIA 服务器（本机无 N 卡）；第 5 天用录屏演示。
- 脚本三件套：`preprocess.py`（原始数据→200 条清洗+统计表）、`train.py`（→训练日志+权重+指标）、`infer.py`（→预测 vs 真值对比）。

**硬约束（全程）**：不得下载 `p2p-full-data` 全库；不写密钥 / 仓库地址 / 录屏外链。

## 第 2 天（08-20）· 组成与选型

**目标**：拆模块、做技术选型，形成可分工、可验收的架构。

**定了什么**

- 主路径 5 模块：① 数据准备 → ② 推理 → ③ 评测 → ④ 演示，外加 ⑤ 微调（扩展）；职责表写清“模块/职责/输入/输出/依赖谁/谁负责”（均本人）。
- 需求-模块对照：必做 1-4 对应 ②③④，必做 5 归档由本人跨模块承担，微调对应 ⑤。

**选型（含放弃理由）**

- 模型 150M（弃 300M/600M/1200M：官方入门默认、最快最省）。
- 环境 uv + Python（弃 pip/conda：与官方 README 一致、可复现）。
- 评测自研脚本（弃官方 validation.py：实测只算困惑度 perplexity，不输出按键准确率 / 鼠标相关系数）。
- 硬件学校 NVIDIA 服务器（弃本机 CPU：无 N 卡）。
- 数据官方 download_data.py（弃手动：格式与切分不易错）。

**风险与对策**：150M 基线可能不达 55% / 0.5（核对口径、如实记录）；服务器排队/断网（提前申请、CPU 兜底）；下载超时（镜像/加速）；自研评测口径错（先小样本人工核对）。

**留给第 3 天**

1. 微调“1 款游戏 2 小时数据”来源，与“不得下载 full-data 全库”冲突 → 待问老师。
2. 200 帧测试集用 toy 还是某款游戏 → 第 3 天定。

## 第 3 天（08-21）· 数据与约定

**目标**：把主路径“要持久化哪些数据 + 脚本怎么调用”写成书面约定，落地仓库。

**摸清的数据格式**（官方源码核对，出处 `elefant/data/video_proto_dataset.py`、`action_label_video_proto_dataset.py`）

- 每视频 = 一个 `.mp4`（192×192）+ 一个 `.proto`（`VideoAnnotation`：`metadata.id` + `frame_annotations` 逐帧标注）。
- 逐帧动作四字段：`keys` / `mouse_buttons` / `mouse_delta_x` / `mouse_delta_y`；另有 `user_action` / `system_action` 区分人类/系统动作。
- 默认 `T=10` 帧/块，demo 用 `T=200`；帧率约 20fps。

**定了什么**

- 数据模型三类对象：原始数据（输入）+ 样本切片（帧 + 动作标注）+ 指标记录（按键准确率 / 鼠标相关系数 / 样本数 / 动作分布），字段附官方出处，并删掉验收用不到的 `env` / `video_id`。
- 脚本口径统一为 preprocess / infer / evaluate（train 降为“扩展·微调”），与第 2 天主路径（推理+评测）一致；统一退出码 0/2/3，最关键一步 `infer.py` 作完整范例，每脚本写成功例 + 失败例。
- 临时假实现边界：第 4-5 天用 `# TODO(假实现)` 标记 + 固定假数据，并约定替换日（preprocess 第 4 天、infer/evaluate 第 5 天），真实实现落地即删。
- 项目备忘 + 当天计划落地仓库。

**回改第 1 天**：立项书/需求规格里的“训练”统一改成“基线推理”（预训练 150M 直接推理，不自己训练）；脚本表由 preprocess/train/infer 改成 preprocess/infer/evaluate + train(扩展)。

**遗留 → 本日已定（不再待问）**

1. 微调数据来源：从 `full-data` 下载“1 款游戏、2 小时数据”子集（老师确认允许，不下载全库）。
2. 200 帧测试集 = `p2p-toy-examples`（必做②“200 条 toy 数据”已锁定）。
3. 口径：200 帧 = 评测集（200 条）；演示用“同一测试片段”（模型输出 vs 人类标注对比，必做4）。

## 第 4 天（08-22）· 工程起步

**目标**：把仓库初始化成能跑、能追溯、可复现的工程骨架（README + .gitignore + 三脚本假实现 + 首跑 + 小步提交 + 审查纪要）。

**做了什么**

- 目录分区：新增 `scripts/`（三脚本），报告/备忘/日志继续作文档区，`data/`、`out/`、`pred/` 等中间产物不入库。
- 根目录 `README.md`：写清安装 / 启动（三条命令）/ 停止 / 环境变量 / 目录职责，假实现阶段仅需 Python 3.11 标准库。
- `.gitignore` 扩充：补敏感文件（.env/密钥）、数据（*.mp4/*.proto）、中间产物（out/、pred/、metrics.json）、编辑器缓存、第四天课件截图，并逐条写原因。
- 三个脚本假实现（纯标准库，标 `# TODO(假实现)`）：preprocess 写 200 条空样本 + stats.csv、infer 写全 0 预测、evaluate 写 0.0%/0.0 指标；退出码 0/2/3。
- 首跑通过：三成功命令 exit 0，`--n_frames -5` exit 2、`--samples 缺失` exit 3。
- 5 次小步提交（README / .gitignore / 三脚本各一），每次只动一件事。

**口径修正**：老师 Day-4 实为“工程起步”（不下载数据/权重），故第 3 天“preprocess 第 4 天换真”顺延到第 5 天，与 infer/evaluate 同期；三脚本本日均保持假实现。

**第 4 天晚 · 提前下载数据/权重（为第 5 天减压）**

老师 Day-4 实为"工程起步"（不要求下载），但为第 5 天减压，当晚把 toy 数据与 150M 权重提前下好（仅 toy + 150M，非 full-data 全库，符合硬约束）。

- 装 uv 0.12.5（官方安装脚本，走 Clash 代理 127.0.0.1:7890）。
- clone 官方仓库 `elefant-ai/open-p2p` 到仓库外 `Desktop/open-p2p`（HEAD `a329d98`），只作下载工具与第 5 天参考，不混入本仓库。
- toy 数据 → `data/toy-examples/`（约 1GB）：3 段视频，每段 `192x192.mp4` + `video.mp4` 原片 + `annotation.proto`；原片 713MB 对 192×192 流水线冗余，暂留待第 5 天确认后删。
- 150M 权重 → `checkpoints/150M/`（2.2GB）：`checkpoint-step=00500000.ckpt`（Lightning 训练 ckpt，含优化器状态故大）+ `model_config.yaml`（官方 README 标已废弃，第 5 天用 `config/policy_model/150M.yaml`）。
- 源仓库均已从 `guaguaa/*` 迁到 `elefantai/*`（旧链接 307 重定向）：toy=`elefantai/p2p-toy-examples`、权重=`elefantai/open-p2p`。
- 下载用 `uv run --with huggingface_hub`（只装 huggingface_hub），绕开官方 pyproject 的 `requires-python ==3.13.2` + torch 重依赖（本机 3.11.9）。
- `.gitignore` 补 `checkpoints/`、`ckpt/`；git 未追踪任何大文件（`.git` 仍约 1MB）。
- 数据冒烟测试：用官方 `video_annotation_pb2.py` 成功解析真 `.proto`（系统 Python 自带 protobuf 7.35.1，免装 torch），核实 fps=20、3 段共约 4.3 万帧、动作字段嵌套（`user_action.keyboard.keys` / `user_action.mouse.mouse_delta_px`）；详见 项目备忘「数据格式」。
- **真 preprocess 已落地**：`scripts/preprocess.py` 换真实实现（`cv2` 读 `192x192.mp4` + 解析 `annotation.proto`），按各视频帧数比例均匀采 200 帧，切帧图 + 取人类动作（`user_action`，忽略 `system_action`）；首跑 exit 0，产出 200 条真样本（`out/samples.json`）+ 统计表（`out/stats.csv`：147 帧按键 / 8 帧鼠标键 / 101 帧鼠标位移 / 30 帧空）+ 200 张 192×192 帧图；失败例 exit 2/3 复验通过。官方 proto schema 复制进 `scripts/proto/`（出处注明），脚本不依赖仓库外路径。

**留给第 5 天**：数据/权重已就位，preprocess 已接真实；剩 infer（读 `checkpoints/150M` 权重）+ evaluate（算两指标）换真，准备录屏演示。

## 第 5 天（08-23）· 主路径贯通

**目标**：把 infer/evaluate 从假实现换真，贯通主路径拿真实指标，交演示说明 + 差距清单 + 报告05。

**做了什么**

- **preprocess 口径修正（关键）**：三处对齐官方——① GT 动作 `system_action` 优先（原只取 `user_action`）；② 采样改单一游戏（1 个 run）连续 200 帧（原 3 视频均匀采样）；③ 读帧 `cv2`(BGR) 改 `torchcodec`(RGB)。
- **infer.py 换真**：加载 150M 权重真推理，`--mode teacher_forcing`（评测，一次前向）/ `free_running`（演示，逐帧自回归，200 帧 × 8 = 1600 次前向）。注入 `_stage3.compile=False` 走 eager（服务器缺 Python.h，flex_attention 的 CUDA 扩展 JIT 会崩）。
- **evaluate.py 换真**：按键准确率（keys 集合精确匹配）+ 鼠标相关系数（x/y Pearson 平均），写 `metrics.json`。
- **真实指标**：teacher_forcing = 按键 **88.0%** / 鼠标 **0.756**（x=0.741 / y=0.772），远超 55%/0.5；评测 200 帧，动作分布 7 种按键 + 123 帧鼠标位移（滑动窗口选段）。

**关键排查（指标 23.5% → 达标）**：真实实现首跑 teacher_forcing 仅 23.5%，根因不是推理 bug 而是第 4 天 preprocess 口径错（只取 `user_action` + 均匀采样）。交叉验证：先把我的 teacher_forcing 推理代码在 p2p-mvp 数据上跑，复现出与其一致的 ~79.5%，证明"推理对、数据错"；修正口径后指标正常。修正是"修 bug 对齐官方权威口径"，非改数据造假。

**选段改进（主动加压）**：修正口径后取"视频开头 200 帧"得 97.5% / 0.842，但该段几乎只按 w 前进、太简单、指标虚高。改为滑动窗口客观选"动作最丰富"的连续 200 帧（按键种类 ×10 + 含鼠标位移/点击加分），选到 7 种按键 + 123 帧鼠标位移的段，teacher_forcing 仍 88.0% / 0.756 达标，结果更有代表性。

**留下的**：`演示说明.md`（可复现操作文档）、报告05（含差距清单 7 条，四要素排第 6-8 天）、README 更新为真实实现、数据模型/接口约定口径修正（system 优先 / 连续 200 帧 / teacher_forcing 评测）。

**留给第 6 天**：微调（train.py + 1 款游戏 2h 数据 + 指标 +8pp/+0.08）、依赖固定（pyproject.toml / uv sync）、proto 许可确认、free_running 录屏演示。
