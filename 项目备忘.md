# 项目备忘（课题六 · 行动克隆游戏智能体）

> 仓库中的短文档，记录相对稳定的约定。每次下达新任务前先读本备忘，不靠「昨天口头说过」。过时条目删除或标为「失效」。

## 主路径与验收

主路径（第 1 天定、第 5 天演示）：toy 原始数据 → 预处理 → 200 帧清洗样本 → 推理(150M) → 预测动作 → 评测 → 指标。

验收指标（必做③）：按键准确率 ≥55%、鼠标相关系数 ≥0.5。

## 必做与不做

- 必做 5 条：① 读懂 150M 模型输入/输出/训练方式；② 处理 200 条 toy 数据出统计表；③ 评测达标（按键≥55%、鼠标≥0.5）；④ 5 条测试推理对比；⑤ 代码 + 指标表 + 3000 字报告。
- 不做：`p2p-full-data` 全库、多模态扩展、生产部署、追 SOTA。

## 技术栈（第 2 天选型）

- 语言/框架：Python + PyTorch（依赖以官方 `requirements.txt` 为准）
- 环境：uv + Python（弃 pip/conda）
- 模型：150M（弃 300M / 600M / 1200M）
- 评测：自研脚本（弃官方 validation.py，其只算困惑度、不出两指标）
- 算力：学校 NVIDIA 服务器（本机无 N 卡，CPU 兜底）
- 工具：CodeBuddy、Git、JupyterLab、Windows 录屏（Win+G）

## 主路径接口索引

| 脚本 | 职责 | 约定文件 |
|---|---|---|
| `preprocess.py` | 原始数据 → 200 帧清洗样本 + 统计表 | 接口约定.md |
| `infer.py` | 150M 权重 + 样本 → 预测动作 | 接口约定.md |
| `evaluate.py` | 预测 + 标注 → 按键准确率 / 鼠标相关系数 | 接口约定.md |
| `train.py`（扩展） | 微调（用 full-data 单款游戏子集，暂不落地） | 接口约定.md |

## 临时假实现边界

第 4-5 天用 `# TODO(假实现)` 标明的占位实现，返回固定假数据；preprocess 第 4 天、infer/evaluate 第 5 天替换为真实；真实实现落地即删标记；归档时带标记者不得当真实实现。

## 已定口径

1. 200 帧测试集用 `p2p-toy-examples`（必做②「200 条 toy 数据」已锁定）。
2. 微调数据来源：从 full-data 下载「1 款游戏 ≤2h」子集（老师确认允许，不下载全库）。

## 硬约束（全程）

- 不得下载 `full-data` 全库（指导书 4.6）；微调仅下载 1 款游戏 ≤2h 子集（已确认允许）。
- 不写密钥、仓库地址、录屏外链。
- 仓库 <10MB（`.gitignore` 挡数据 / 权重 / 老师课件）。
