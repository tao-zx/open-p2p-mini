# 项目备忘（课题六 · 行动克隆游戏智能体）

> 仓库中的短文档，记录相对稳定的约定。每次下达新任务前先读本备忘，不靠“昨天口头说过”。过时条目删除或标为“失效”。

## 主路径与验收

主路径（第 1 天定、第 5 天演示）：toy 原始数据 → 预处理 → 200 帧清洗样本 → 推理(150M) → 预测动作 → 评测 → 指标。

验收指标（必做③）：按键准确率 ≥55%、鼠标相关系数 ≥0.5。

## 必做与不做

- 必做 5 条：① 读懂 150M 模型输入/输出/训练方式；② 处理 200 条 toy 数据（与训练/微调数据隔离）出统计表；③ 评测达标（按键≥55%、鼠标≥0.5）；④ 同一测试片段模型输出 vs 人类标注对比（第 5 天录屏）；⑤ 代码 + 指标表 + 3000 字报告。
- 不做：`p2p-full-data` 全库、多模态扩展、生产部署、追 SOTA。

## 技术栈（第 2 天选型）

- 语言/框架：Python + PyTorch（依赖以官方 `requirements.txt` 为准）
- 环境：uv + Python（弃 pip/conda）
- 模型：150M（弃 300M / 600M / 1200M）
- 评测：自研脚本（弃官方 validation.py，其只算困惑度、不出两指标）
- 算力：学校 NVIDIA 服务器（本机无 N 卡，CPU 兜底）
- 工具：CodeBuddy、Git、JupyterLab、Windows 录屏（Win+G）

## 主路径接口索引

| 脚本 | 职责 | 约定文件 |
|---|---|---|
| `preprocess.py` | 原始数据 → 200 帧清洗样本 + 统计表 | 接口约定.md |
| `infer.py` | 150M 权重 + 样本 → 预测动作 | 接口约定.md |
| `evaluate.py` | 预测 + 标注 → 按键准确率 / 鼠标相关系数 | 接口约定.md |
| `train.py`（扩展） | 微调（用 full-data 1 款游戏 2 小时数据子集，暂不落地） | 接口约定.md |

## 本地数据与权重（第 4 天晚已下）

- toy 数据：`data/toy-examples/`（3 段视频，每段 `192x192.mp4` + `video.mp4` 原片 + `annotation.proto`；约 1GB，原片 713MB 对 192×192 流水线冗余，第 5 天确认后删）。
- 150M 权重：`checkpoints/150M/checkpoint-step=00500000.ckpt`（2.2GB，Lightning 训练 ckpt 含优化器状态）+ `model_config.yaml`（infer 实际用它跑通，第 5 天验证有效）。
- 官方仓库在仓库外 `Desktop/open-p2p`（参考/工具）；数据源均已迁至 `elefantai/*`（toy=`elefantai/p2p-toy-examples`、权重=`elefantai/open-p2p`）。

## 数据格式（第 4 天晚冒烟测试核实）

- `.proto` 用官方 `video_annotation_pb2.py` 解析（系统 Python 自带 protobuf 即可）；该 schema 已复制进 `scripts/proto/`（出处注明），`preprocess.py` 直接 `from proto import ...`，绕过官方 `elefant/data/__init__` 的重依赖。
- 顶层 `VideoAnnotation`：`metadata.id`（=目录 UUID）、`metadata.frames_per_second`（=20）、`frame_annotations[]`。
- 每帧 `FrameAnnotation`：`user_action` / `system_action`（均为 `LowLevelAction`）。动作字段**嵌套**——按键 `keyboard.keys`（字符串列表）；鼠标 `mouse.mouse_delta_px.x/.y`、`mouse.buttons_down`。
- **GT 动作口径（第 5 天修正）**：`system_action` 优先——`system_action.is_known` 用 system，否则回退 `user_action`，都未知则空动作（官方 `action_label_video_proto_dataset.py:168-189`）。
- 3 段视频帧数 2456 / 18790 / 21930（共约 4.3 万帧）；**"200 帧测试集"= 选 1 个 run，用滑动窗口选"动作最丰富"的连续 200 帧**（按键种类 ×10 + 含鼠标位移/点击加分；单一游戏，与训练/微调隔离），不是整段就 200 帧、不是多视频均匀采样、更不是取开头简单段。

## 临时假实现边界

第 4-5 天用 `# TODO(假实现)` 标明的占位实现，返回固定假数据。三个脚本均已替换为真实实现并删标记：preprocess 第 4 天晚、infer/evaluate 第 5 天。归档时带标记者不得当真实实现。

## 已定口径

1. 200 帧测试集用 `p2p-toy-examples`（必做②“200 条 toy 数据”已锁定）。
2. 微调数据来源：从 full-data 下载“1 款游戏、2 小时数据”子集（老师确认允许，不下载全库）；微调指标 = 按键准确率 +8 个百分点 或 鼠标相关系数 +0.08。

## 待决问题

1. 微调数据具体选哪一款游戏、如何按子集下载——第 6 天做微调前定（指标已定：按键 +8pp 或 鼠标 +0.08）。

## 硬约束（全程）

- 不得下载 `full-data` 全库（指导书 4.6）；微调仅下载 1 款游戏、2 小时数据子集（已确认允许）。
- 不写密钥、仓库地址、录屏外链。
- 仓库 <10MB（`.gitignore` 挡数据 / 权重 / 老师课件）。
