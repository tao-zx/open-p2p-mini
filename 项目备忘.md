# 项目备忘（课题六 · 行动克隆游戏智能体）

> 仓库中的短文档，记录相对稳定的约定。每次下达新任务前先读本备忘，不靠“昨天口头说过”。过时条目删除或标为“失效”。

## 主路径与验收

主路径（第 1 天定、第 5 天演示）：toy 原始数据 → 预处理 → 200 帧清洗样本 → 推理(150M) → 预测动作 → 评测 → 指标。

验收指标（必做③）：按键准确率 ≥55%、鼠标相关系数 ≥0.5。

## 必做与不做

- 必做 5 条：① 跑通官方 150M 预训练模型的推理脚本，README 可复现；② 处理 200 条 toy 数据（与训练/微调数据隔离）出统计表；③ 评测达标（按键≥55%、鼠标≥0.5）；④ 同一测试片段模型输出 vs 人类标注对比（最后一天录屏）；⑤ 代码 + 指标表 + 3000 字报告。
- 不做：`p2p-full-data` 全库、多模态扩展、生产部署、追 SOTA。

## 技术栈（第 2 天选型）

- 语言/框架：Python + PyTorch（依赖以官方 `requirements.txt` 为准）
- 环境：uv + Python（弃 pip/conda）
- 模型：150M（弃 300M / 600M / 1200M）
- 评测：自研脚本（弃官方 validation.py，其只算困惑度、不出两指标）
- 算力：学校 NVIDIA 服务器（本机无 N 卡，CPU 兜底）
- 工具：CodeBuddy、Git、JupyterLab、Windows 录屏（Win+G）

## 主路径接口索引

| 脚本 | 职责 | 约定文件 |
|---|---|---|
| `preprocess.py` | 原始数据 → 200 帧清洗样本 + 统计表 | 接口约定.md |
| `infer.py` | 150M 权重 + 样本 → 预测动作 | 接口约定.md |
| `evaluate.py` | 预测 + 标注 → 按键准确率 / 鼠标相关系数 | 接口约定.md |
| `train.py`（扩展） | 微调（full-data 1 款游戏 2 小时子集；第 6 天已落地真实实现） | 接口约定.md |
| `prepare_finetune_data.py`（扩展） | 微调数据准备（下 batch 子集 → 按游戏过滤 → 排除测试段 → 截 2h；第 6 天已落地） | 接口约定.md |

## 本地数据与权重（第 4 天晚已下）

- toy 数据：`data/toy-examples/`（3 段视频，每段 `192x192.mp4` + `video.mp4` 原片 + `annotation.proto`；约 1GB，原片 713MB 对 192×192 流水线冗余，第 5 天确认后删）。
- 150M 权重：`checkpoints/150M/checkpoint-step=00500000.ckpt`（2.2GB，Lightning 训练 ckpt 含优化器状态）+ `model_config.yaml`（infer 实际用它跑通，第 5 天验证有效）。
- 官方仓库在仓库外 `Desktop/open-p2p`（参考/工具）；数据源均已迁至 `elefantai/*`（toy=`elefantai/p2p-toy-examples`、权重=`elefantai/open-p2p`）。

## 数据格式（第 4 天晚冒烟测试核实）

- `.proto` 用官方 `video_annotation_pb2.py` 解析（系统 Python 自带 protobuf 即可）；该 schema 已复制进 `scripts/proto/`（出处注明），`preprocess.py` 直接 `from proto import ...`，绕过官方 `elefant/data/__init__` 的重依赖。
- 顶层 `VideoAnnotation`：`metadata.id`（=目录 UUID）、`metadata.frames_per_second`（=20）、`frame_annotations[]`。
- 每帧 `FrameAnnotation`：`user_action` / `system_action`（均为 `LowLevelAction`）。动作字段**嵌套**——按键 `keyboard.keys`（字符串列表）；鼠标 `mouse.mouse_delta_px.x/.y`、`mouse.buttons_down`。
- **GT 动作口径（第 5 天修正）**：`system_action` 优先——`system_action.is_known` 用 system，否则回退 `user_action`，都未知则空动作（官方 `action_label_video_proto_dataset.py:168-189`）。
- 3 段视频帧数 2456 / 18790 / 21930（共约 4.3 万帧）；**"200 帧测试集"= 选 1 个 run，用滑动窗口选"动作最丰富"的连续 200 帧**（按键种类 ×10 + 含鼠标位移/点击加分；单一游戏，与训练/微调隔离），不是整段就 200 帧、不是多视频均匀采样、更不是取开头简单段。

## 临时假实现边界

第 4-5 天用 `# TODO(假实现)` 标明的占位实现，返回固定假数据。三个脚本均已替换为真实实现并删标记：preprocess 第 4 天晚、infer/evaluate 第 5 天。归档时带标记者不得当真实实现。

## 已定口径

1. 200 帧测试集用 `p2p-toy-examples`（必做②”200 条 toy 数据”已锁定）。toy 实际是 full-data 的子集（3 个 toy UUID 全在 full-data 里）；当前测试集 = toy 第一段 = `call-of-duty-mobile`（帧号 1990~2189 连续 200 帧）。
2. 微调数据来源：从 full-data 下载”1 款游戏、2 小时数据”子集（老师确认允许，不下载全库）；微调指标 = 按键准确率 +8 个百分点 或 鼠标相关系数 +0.08。**微调游戏（第 6 天）**：先试 COD `call-of-duty-mobile`（与测试集同游戏），两轮退化（88%→81.5%/83%）后换到 GTA `grand-theft-auto-san-andreas`（基线 69.5%），也退化/持平（best step400 +1.5pp，落在噪声内）。第 7 天换方法（冻 tokenizer+cosine）best +0.5pp、第 8 天 LR1e-5 均值 −0.8pp（单点 best +4.5pp 为采样噪声），四波均未达 +8pp。**结论：2h 单游戏子集微调 150M 不升反降、+8pp 结构不可达（scaling law 背书），已归档为合法科学发现。**
3. 依赖固定：根目录 `pyproject.toml`（基础 numpy/pillow/protobuf + `gpu` extra 的 torch/torchcodec/lightning）+ `依赖清单.md`（三层依赖/环境矩阵/复现 recipe）；`elefant` 官方包非 pip，由 `infer.py --official-repo` 指路。

## 第 6 天计划（拆两块并行 + harness，老师 Day-6 要求）

第 6 天按老师要求，把剩余差距拆成**两块并行**实现，每块可独立自测，再交叉阅读；同时搭建 harness 交接文档体系，全部写进报告06。

- **块 1：依赖固定**（差距 3，中）——补 `pyproject.toml` + 依赖清单，`uv sync` 一键复现；自测 = 干净环境跑通三脚本。
- **块 2：微调**（差距 1，高，跨第 6-7 天）——下 full-data 1 款游戏 2h 子集（选游戏见“待决问题”第 1 条，第 6 天做微调前定），落地 `train.py`，微调 150M，按键 +8pp 或 鼠标 +0.08；自测 = 训练跑通 + 指标对比。
- **交叉阅读**：本人扮演 reviewer，逐块审另一块改动、跑对方自测命令、留问题单（位置 / 现象 / 建议 / 是否已修）。
- **harness 交付**：根目录 `CLAUDE.md`（总则 + 索引 + 口径速览）已建，是交接文档总入口，新对话自动读取；报告06 必写“总则（≤100 行）+ 索引（几十行）”。

**执行顺序**：块 1 → 块 2 → 交叉阅读 → 开发日志 → 报告06；块 2 第一步先定微调选哪款游戏（见“待决问题”第 1 条）。

**第 6 天进度（收尾）**：块 1 落地 `pyproject.toml` + `依赖清单.md`（`uv sync --extra gpu` 服务器自测通过）；块 2 全链跑通——COD 2h 微调两轮退化（88%→81.5%/83%）→ 换 GTA 2h 微调（best step400 +1.5pp，700 步 66%）、均未达 +8pp；交叉阅读 3 条问题单已关；报告06 / 开发日志 / 项目备忘已同步。

**报告 06 交付物**：两块完成说明 + 各自自测步骤与结果 + 交叉阅读问题单 + harness 总则与索引。

## 第 6-9 天待办（差距清单）

1. **微调落地**（train.py + 指标提升，高，第 6-7 天）：下 full-data 1 款游戏 2h 子集，微调 150M，按键 +8pp 或 鼠标 +0.08；输出训练日志、微调权重、`train.py`、微调前后指标对比表。
2. **录屏演示**（必做④，高，最后一天）：teacher_forcing 口径”模型输出 vs 人类标注”逐帧对比（对比卡片已生成），产出录屏 + 对比说明。
3. **依赖固定**（pyproject.toml / uv sync，中，第 6 天）：补依赖清单，让 venv 能一键重建，第二人 `uv sync` 即可复现。

## 待决问题

1. ~~微调选哪款游戏、如何按子集下载~~ **已定（第 6 天）**：先选 `call-of-duty-mobile`（与测试集同游戏，域匹配最好；full-data 里 80h/1230 条），两轮退化后**换到 GTA `grand-theft-auto-san-andreas`**（基线 69.5% 提升空间更大）。**下载方式已摸清**：full-data 是 545 个 batch 打包（`dataset/batch_00001~00545.tar.gz`，每批 ~200 条、末批 51 条），batch 按 **UUIDv7 时间序**分块（`batch_N ≈ 时间序第 200×(N-1)~200×N 条`）；`data_metadata.parquet` 给 UUID→游戏+帧数，`id`/`filepath` 是 UUIDv7（前 48 位=时间戳，字符串排序=时间序）。COD 最密集 **batch 167（45 条 COD / 3.1h）**；GTA 最密集 **batch 285（7 条 GTA / 2.99h）**。**排除测试段**：COD 测试段 UUID `01993060-06a0-7352-ac0b-d77d50a1aba7`（toy 第一段，2456 帧，batch 317）；GTA 测试段 UUID `01993bb8-12e7-78a1-b661-0c0c3a258e21`（batch 332，与微调 batch 285 无泄漏）。
2. ~~proto 许可~~ **已定（第 6 天）**：官方 open-p2p 仓库为 **MIT License**，允许复制 schema 入库、保留出处即可（`scripts/proto/__init__.py` 已注明）。
3. `infer.py` 注入 `_stage3.compile=False` 走 eager 的 workaround（服务器缺 Python.h），评估环境升级后能否移除（未动，仅影响性能）。
4. ~~train.py 的 `finetuned.ckpt` 坑~~ **已修（第 7 天）**：`trainer.save_checkpoint(final_path)` 写的 `finetuned.ckpt` 在 `infer.py` 加载时报 `block_mask 在 cpu、q/k/v 在 cuda`（flex_attention device 不匹配）；实际能跑的是 ModelCheckpoint 的 `checkpoint-00000700.ckpt`（文件名格式 `checkpoint-{step:08d}`，无“step=”）。第 7 天已修：train.py 不再写 `finetuned.ckpt`，最终权重改指 ModelCheckpoint 同款快照（`checkpoint-{step:08d}`），评测用 `checkpoints/` 里的周期快照。

## 硬约束（全程）

- 不得下载 `full-data` 全库（指导书 4.6）；微调仅下载 1 款游戏、2 小时数据子集（已确认允许）。
- 不写密钥、仓库地址、录屏外链。
- 仓库 <10MB（`.gitignore` 挡数据 / 权重 / 老师课件）。
