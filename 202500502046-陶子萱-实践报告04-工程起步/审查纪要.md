# 审查纪要（第 4 天 · 工程起步脚手架）

> 记录谁审查了哪次改动、发现了什么、是否已修。单人完成：生成（助手）与审查（本人）分两段时间进行——先由助手生成脚手架并提交，本人再逐文件通读、逐条核对，本纪要记录"审查"这一段的通读结果。

## 基本信息

- **审查人**：陶子萱
- **审查时间**：2026-08-22（与生成为同一天，但分两段：先生成、后通读）
- **所看分支**：main
- **所看提交**：第 4 天全部 12 次提交（`cded9d5` … `828e2ab` 及报告/留痕提交）

## 通读范围（逐文件，非只看 git log）

| 文件 | 是否通读 | 结论 |
|---|---|---|
| `README.md` | ✅ | 环境要求 / 安装 / 启动三命令 / 停止齐全，可跟做 |
| `.gitignore` | ✅ | 挡数据 / 权重 / 敏感文件 / 中间产物，逐条写原因 |
| `scripts/preprocess.py` | ✅ | 真实实现，读 mp4 + proto，首跑 exit 0 |
| `scripts/infer.py` | ✅ | 假实现（全 0 预测），已标 `# TODO(假实现)` |
| `scripts/evaluate.py` | ✅ | 假实现（0.0% / 0.0），已标 `# TODO(假实现)` |
| `scripts/proto/`（3 文件） | ✅ | 官方 schema 复制入库，出处已注明 |
| `数据模型.md` / `接口约定.md` | ✅ | 200=子集、user/system 出处已修正 |

## 检查项与结论

| 检查项 | 结论 |
|---|---|
| 目录结构 | 代码 / 文档 / 数据分区清楚，无无关模板 |
| 配置与密钥 | 无密钥 / 密码 / 仓库地址入库 |
| 与昨日约定一致 | 脚本名 / 参数 / 退出码 0/2/3 符合第 3 天接口约定 |
| 依赖 | preprocess 真实现多一个 `cv2`；infer/evaluate 仍仅标准库 |
| 假实现边界 | 已标 `# TODO(假实现)` + 约定替换日（infer/evaluate 第 5 天） |
| 首跑 | 三步 exit 0，失败例 exit 2 / 3 均正确 |

## 问题列表与处理

| # | 问题 | 严重度 | 是否已修 / 处理 |
|---|---|---|---|
| 1 | `evaluate.py` 把 `metrics.json` 写到当前目录（无 `--out` 参数），与 preprocess/infer 的输出位置不统一 | 低 | 未改，顺延第 5 天换真时补 `--out`（假实现阶段不产真指标，影响小） |
| 2 | 依赖未用文件固定（`cv2` 无 requirements / pyproject.toml） | 中 | 未改，第 5 天 `uv sync` 时补 pyproject.toml（README 已注明） |
| 3 | `scripts/proto/` 为官方生成代码，出处已注明，但其上游许可（官方 pyproject 标 Private）待归档前确认 | 中 | 部分：出处已注明；许可第 5 天归档前确认 |
| 4 | 200 帧中约 30 帧为空帧（无按键无鼠标），评测时是否跳过 / 如何计入口径待第 5 天定 | 低 | 已记录在 `数据模型.md` 说明 2（`is_known=false` 跳过） |
| 5 | 提交纪律：`36e0720` 一次提交同步了 5 个文档，粒度偏粗 | 低 | 已发现；后续提交拆细 |

## 是否合入主分支

本次通读未发现阻断性问题（无密钥泄露、无跑不通、无与昨日约定冲突）；问题 1–5 均为"顺延第 5 天"或"提交粒度"类，不影响当前可运行。**结论：可合入 main，问题记入待办随第 5 天处理。**

## 待办（随第 5 天）

- [ ] `evaluate.py` 补 `--out`，统一输出位置
- [ ] 补 pyproject.toml / 依赖清单（cv2、后续 torch）
- [ ] 归档前确认 `scripts/proto` 上游许可
- [ ] 定 200 帧评测口径（空帧 / `is_known` 是否跳过）
