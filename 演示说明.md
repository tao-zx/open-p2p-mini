# 演示说明（第 5 天 · 主路径贯通）

> 目的：让第二人**只按本文档**就能独立复现主路径（toy 数据 → 预处理 → 推理 → 评测 → 指标），无需作者口头补步骤。按老师要求写清：**环境前提、编号步骤、每步预期现象、常见失败检查**。

## 一、环境前提

### 1. 算力与环境
- **GPU 服务器**（本课题用 NVIDIA Quadro RTX 6000 24GB，CUDA）。
- **官方 open-p2p 仓库**（含 `elefant` 包）+ 其 Python 虚拟环境（Python 3.12，torch 2.11+cu128、lightning、torchcodec、protobuf）。
- 说明：`preprocess` 用 torchcodec 读帧、`infer` 加载 150M CUDA 权重做 GPU 前向，**本机无 N 卡的 Windows 环境无法完整跑通**，须在 GPU 服务器执行。

### 2. 工作目录与文件（运行前提；数据/权重不入库）
工作目录 `/root/workspace/taozixuan/`（符合老师"数据存放（必读）"：长期文件统一放 `/root/workspace` 及其子目录）：

- `data/p2p-toy-examples/`：toy 数据，3 段视频，每段 `192x192.mp4` + `annotation.proto`（来自官方 `elefantai/p2p-toy-examples`）。
- `checkpoints/150M/`：150M 权重 `checkpoint-step=00500000.ckpt`（2.2GB）+ `model_config.yaml`（来自官方 `elefantai/open-p2p`）。
- `scripts/`：`preprocess.py` / `infer.py` / `evaluate.py`（均已接真实实现）。
- 以上文件获取（下载）步骤见 `开发日志.md` 第 4 天。

### 3. 两个推理模式（必读，决定验收口径）
`infer.py` 的 `--mode` 有两个值，用途不同：

- **`teacher_forcing`（默认，评测指标用）**：把 200 帧真实动作填进历史窗口，**一次前向**预测全部帧，再解码算指标。这是"行为克隆标准评估"，验收阈值（按键准确率 ≥55%、鼠标相关系数 ≥0.5）即此口径。
- **`free_running`（演示录屏用）**：模型**逐帧自主预测**，每帧自回归解 8 个动作 token（200 帧 × 8 = 1600 次前向），误差会累积，key_acc 天然低于 teacher_forcing——**这不是 bug，是"自主运行"的正常表现**。

## 二、主路径步骤

### 步骤 1：数据准备（preprocess）
```bash
cd /root/workspace/taozixuan
/root/workspace/open-p2p/.venv/bin/python scripts/preprocess.py \
  --data_dir data/p2p-toy-examples --out_dir out --n_frames 200
```
**预期现象**：
```
[OK] 处理 200 帧 → 产出 200 条样本
[OK] 写出 out/samples.json、out/stats.csv
```
**产出**：`out/samples.json`（200 条，每帧 `keys` / `mouse_buttons` / `mouse_delta_x` / `mouse_delta_y` / `is_known` / `is_user_action`）、`out/stats.csv`（动作分布统计）、`out/frames/`（200 张 192×192 帧图）。

**常见失败检查**：
- `[ERROR] 数据目录不存在` → `--data_dir` 路径错，确认 `data/p2p-toy-examples/` 存在。
- `[ERROR] 读视频失败` → venv 缺 torchcodec，务必用 `/root/workspace/open-p2p/.venv/bin/python`（系统 python 无此包）。

### 步骤 2：推理（infer，teacher_forcing 算指标）
```bash
/root/workspace/open-p2p/.venv/bin/python scripts/infer.py \
  --weights checkpoints/150M/checkpoint-step=00500000.ckpt \
  --samples out/samples.json --out pred \
  --official-repo /root/workspace/open-p2p --mode teacher_forcing
```
**预期现象**：
```
[OK] 推理 200 帧完成
[OK] 写出 pred/predictions.json
```
**产出**：`pred/predictions.json`（200 帧每帧 `keys` / `mouse_buttons` / `mouse_delta_x` / `mouse_delta_y`）。

**常见失败检查**：
- `[ERROR] 权重文件不存在` → `--weights` 路径错，确认 `checkpoints/150M/checkpoint-step=00500000.ckpt` 存在。
- `[ERROR] 缺少官方 elefant 依赖` → 未给 `--official-repo` 或 venv 未装 open-p2p。
- 日志出现 `flex_attention called without torch.compile` → 已按设计关闭 torch.compile 走 eager（服务器缺 Python.h，无法 JIT 编译 CUDA 扩展），属正常，仅略慢。

### 步骤 3：评测（evaluate）
```bash
/root/workspace/open-p2p/.venv/bin/python scripts/evaluate.py \
  --pred pred/predictions.json --label out/samples.json --out metrics.json
```
**预期现象**（真实结果）：
```
[OK] 按键准确率 = 97.5% （阈值 55%，评测 200 帧）
[OK] 鼠标相关系数 = 0.842 （阈值 0.5，x=0.808 / y=0.876）
[OK] 写出 metrics.json
```
**产出**：`metrics.json`（`key_accuracy`=0.975、`mouse_correlation`=0.8417，均超阈值）。

**常见失败检查**：
- `[ERROR] 没有可评测的帧` → 标注帧全 `is_known=false`，检查 `out/samples.json` 是否正常产出。

### 步骤 4（演示用）：推理（free_running）+ 对比
```bash
/root/workspace/open-p2p/.venv/bin/python scripts/infer.py \
  --weights checkpoints/150M/checkpoint-step=00500000.ckpt \
  --samples out/samples.json --out pred_free \
  --official-repo /root/workspace/open-p2p --mode free_running
```
**预期现象**：跑约 10–15 分钟（1600 次前向），每 20 帧打一行 `[进度] N/200 帧`，最后 `[OK] 写出 pred_free/predictions.json`。

再评测自由运行指标：
```bash
/root/workspace/open-p2p/.venv/bin/python scripts/evaluate.py \
  --pred pred_free/predictions.json --label out/samples.json --out metrics_free.json
```
**预期现象**（真实结果）：`key_accuracy` = **81.5%**（低于 teacher_forcing 的 97.5%，但仍超 55% 阈值）、`mouse_correlation` = **0.247**（x=0.258 / y=0.236，明显低于 0.5）。这说明"自主运行"时误差会累积——对离散按键影响小（81.5% 仍达标），对连续鼠标位移影响大（相关系数从 0.842 掉到 0.247）。**验收指标用 teacher_forcing（步骤 2-3），free_running 仅作演示"模型自主玩"。**

## 三、演示（必做4：同一测试片段模型输出 vs 人类标注）

录屏演示**同一条 200 帧测试片段**上，模型预测与人类标注的对比：

1. 依次执行步骤 1 → 2 → 3，截取每步 `[OK]` 输出与 `metrics.json` 内容（验收指标 97.5% / 0.842）。
2. 逐帧对照 `out/samples.json`（人类标注 GT）与 `pred/predictions.json`（模型预测）的 `keys` / `mouse_delta_x/y`，展示"看画面 → 猜动作"与真人操作的一致/差异。
3. 可补充步骤 4 的 free_running 演示，说明"自主运行"与"teacher-forcing 评估"的口径区别。
