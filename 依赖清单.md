# 依赖清单（块 1 · 依赖固定）

> 用途：说清主路径三脚本分别依赖什么、怎么 `uv sync` 一键复现、在什么环境能跑。脚本接口/退出码见 `接口约定.md`。

## 一、分层

| 层 | 内容 | 谁需要 | 怎么装 |
|---|---|---|---|
| 第 1 层 | `numpy`、`pillow`、`protobuf`（本项目 `pyproject.toml` 声明） | `preprocess.py`（numpy/pillow/protobuf）、`infer.py`（numpy/pillow） | `uv sync` |
| 第 2 层 | `torch`(cu128)、`torchcodec`、`lightning`（`pyproject.toml` 的 `gpu` extra） | `preprocess.py`（torchcodec 读帧）、`infer.py`（torch/lightning） | `uv sync --extra gpu` |
| 第 3 层 | `elefant` 官方包（open-p2p 仓库源码，非 pip） | `infer.py`（`import elefant.*`）、`train.py`（微调） | 克隆官方仓库 + `--official-repo` 指路（见下） |

`evaluate.py` 只依赖 Python 标准库，任意 Python 3.11+ 环境可跑。

## 二、环境矩阵

| 脚本 | 任意 Python 3.11+（本机 Windows） | GPU Linux（第 2/3 层齐全） |
|---|---|---|
| `evaluate.py` | ✅ 可跑 | ✅ |
| `preprocess.py` | ⚠️ 仅参数校验/扫目录（torchcodec 读帧需 GPU Linux） | ✅ 完整 |
| `infer.py` | ❌（需 GPU + elefant） | ✅ 完整 |
| `train.py`（微调，扩展） | ❌ | ✅ |

## 三、一键复现 recipe

```bash
# 0) 官方仓库（第 3 层；已 clone 过则跳过）
git clone https://github.com/elefant-ai/open-p2p   # 仓库外，如 /root/workspace/open-p2p

# 1) 本项目依赖（第 1+2 层，只够 preprocess.py / evaluate.py）
uv sync --extra gpu

# 1.5) 仅 infer.py / train.py 才需要：第 3 层 elefant 的依赖（wandb/transformers 等 60+ 项）
#      由官方仓库自己的 venv 提供：cd <open-p2p仓库> && uv sync

# 2) 跑主路径（preprocess/evaluate 用本项目 venv；infer 用官方 venv + --official-repo 指路）
python scripts/preprocess.py --data_dir data/p2p-toy-examples --out_dir out --n_frames 200
python scripts/infer.py --weights checkpoints/150M/checkpoint-step=00500000.ckpt \
  --samples out/samples.json --out pred --official-repo <open-p2p仓库> --mode teacher_forcing
python scripts/evaluate.py --pred pred/predictions.json --label out/samples.json --out metrics.json

# 3) 微调（扩展，第 6 天真实命令；需先跑完主路径 + 第 3 层 elefant venv。
#    `<测试段samples.json>` = 该游戏测试段的 preprocess 产物，与训练数据隔离、防泄漏）
#    a) 准备 GTA 2h 子集（batch 285；下载走 HF 镜像或直连 CDN，见 CLAUDE.md 口径速览）
python scripts/prepare_finetune_data.py --start 285 --end 285 \
  --game grand-theft-auto-san-andreas --hours 2 \
  --exclude 01993bb8-12e7-78a1-b661-0c0c3a258e21 --out_dir data/gta-2h

#    b) 微调 150M（LR 3e-5 防遗忘 / 700 步）
python scripts/train.py --weights checkpoints/150M/checkpoint-step=00500000.ckpt \
  --data_dir data/gta-2h --out_dir out/finetune-gta --official-repo <open-p2p仓库> \
  --n_steps 700 --lr 3e-5 --freeze_steps 0

#    c) 评测微调后权重（用 ModelCheckpoint 周期快照，不是 finetuned.ckpt）
python scripts/infer.py --weights out/finetune-gta/checkpoints/checkpoint-00000700.ckpt \
  --samples <测试段samples.json> --out pred-finetune --official-repo <open-p2p仓库> \
  --config checkpoints/150M/model_config.yaml
python scripts/evaluate.py --pred pred-finetune/predictions.json --label <测试段samples.json> --out metrics-finetune.json

#    d) 逐 checkpoint 扫描（找 best step，对比基线看 +8pp / +0.08）
for ckpt in out/finetune-gta/checkpoints/checkpoint-*.ckpt; do
  step=$(basename "$ckpt" .ckpt | sed 's/checkpoint-//')
  python scripts/infer.py --weights "$ckpt" --samples <测试段samples.json> \
    --out pred-sweep --official-repo <open-p2p仓库> --config checkpoints/150M/model_config.yaml
  python scripts/evaluate.py --pred pred-sweep/predictions.json --label <测试段samples.json> --out "metrics-$step.json"
  echo "step $step: $(cat "metrics-$step.json")"
done
```

> `elefant` 不是 pip 包（官方仓库 `classifiers=["Private :: Do Not Upload"]`），故不写进 `pyproject.toml` 的依赖，避免 `uv sync` 被官方 `requires-python ==3.13.2` + 60 余项重依赖（transformers/git 依赖/rust workspace 成员）卡死。官方依赖树由官方仓库自己的 `uv sync` 装，本项目只在运行时用 `--official-repo` 指到它。

## 四、与官方口径的对齐（出处）

- torch 装自 `pytorch-cuda-stable`（cu128）、torchcodec 装自 `pytorch-cpu-stable`：对照官方 `open-p2p/pyproject.toml` 的 `[tool.uv.sources]`。
- `protobuf>=5.29`：对照官方 `pyproject.toml`（`protobuf>=5.29.2`）；本机 proto 解析实测用系统 protobuf 7.35.1（`开发日志.md` 第 4 天）。
- `scripts/proto/*_pb2.py` 复制自官方 `elefant/data/proto/`，MIT 许可（出处见 `scripts/proto/__init__.py`），标准 proto3，protobuf 5.x+ 运行时即可解析。

## 五、自测（块 1 验收：干净环境跑通三脚本）

在 GPU 服务器干净 venv 执行（第二人视角）：

1. `uv sync --extra gpu` → 无报错，装齐第 1/2 层。
2. 三脚本各跑一遍成功命令 + 失败例（退出码 2/3），结果与 `演示说明.md` 一致。
3. 本机（无 GPU）仅验证 `evaluate.py` 纯标准库可跑 + `pyproject.toml` 语法合法。