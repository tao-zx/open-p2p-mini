# 课题六 · 行动克隆游戏智能体（Open P2P）— 项目总则与索引（harness）

> 本文件是给 AI 助手（Claude Code）自动读取的 harness「总则 + 索引」。新对话开工前先读本文件，再按索引去读需要的具体文档，不必全文通读。每次对话结束，把本次重要变化凝练更新回对应文档。

## 一、总则（开发原则 + 雷区）

### 开发原则
1. **诚实优先**：指标、结果一律如实记录，不造假、不改数据；达标靠「修 bug 对齐官方口径」，不是改数据。
2. **小步提交**：每次 commit 只动一件事，注释写清「改了什么、为何改」。
3. **遵守书面约定**：脚本输入输出以 `接口约定.md` 为准，数据字段以 `数据模型.md` 为准；口径变更须同步改文档与 `项目备忘.md`。
4. **不拍脑袋**：结论附出处（官方源码 `文件:行号`）；缺资料主动向用户索要，不猜。
5. **单人完成**：本项目独立完成，A/B 两块的「交叉阅读」由本人扮演 reviewer 执行。

### 雷区（不能干）
- 不下载 `p2p-full-data` 全库；微调只下 1 款游戏、2 小时数据子集（指导书 4.6）。
- 不在报告 / commit / 文档里写：服务器地址、密码、仓库地址、录屏外链。
- 仓库 < 10MB；数据、权重、中间产物不入库（`.gitignore` 已挡）。
- 不把参考项目 `p2p-mvp` 的代码与口径和本项目弄混。

## 二、索引（看什么、在哪个文件）

| 想看什么 | 文件 |
|---|---|
| 稳定约定、已定口径、硬约束、待办/待决 | `项目备忘.md` |
| 按天做了什么、定了什么、留了什么 | `开发日志.md` |
| 脚本接口与退出码（0 成功 / 2 参数错 / 3 数据错） | `202500502046-陶子萱-实践报告03-数据与约定/接口约定.md` |
| 数据字段与格式 | `202500502046-陶子萱-实践报告03-数据与约定/数据模型.md` |
| 主路径复现步骤（环境 + 命令 + 预期现象 + 失败检查） | `演示说明.md` |
| 启动 / 停止 / 环境要求 | `README.md` |
| 依赖与环境复现（uv sync 分层） | `pyproject.toml` + `依赖清单.md` |
| 各天老师要求（截图/OCR/录音） | `要求/` |

## 三、关键口径速览（高频，防踩坑）

- **验收指标**：按键准确率 ≥55%（keys 集合精确匹配）、鼠标相关系数 ≥0.5（x/y Pearson 平均）；当前基线 **88.0% / 0.756**（x=0.741 / y=0.772）。
- **GT 动作口径**：`system_action` 优先，未知回退 `user_action`，都未知则空动作。
- **测试集**：单一游戏（1 个 run）连续 200 帧，滑动窗口选「动作最丰富」段（按键种类 ×10 + 鼠标位移/点击加分）；当前测试集 = toy 第一段 = `call-of-duty-mobile`（toy 实为 full-data 子集）。
- **微调**：选 `call-of-duty-mobile`（与测试集同游戏，80h/1230 条，排除 3 个 toy UUID 取 2h）；指标 = 按键 +8pp 或 鼠标 +0.08；不得下载全库。
- **读帧**：`torchcodec`（RGB），不用 `cv2`（BGR）。
- **推理模式**：`teacher_forcing`（验收，一次前向）/ `free_running`（演示，逐帧自回归，误差累积、天然偏低）。
- **环境**：GPU 服务器 + open-p2p venv（torch 2.11+cu128、torchcodec、protobuf）；本机 Windows 无 N 卡，跑不了真推理。依赖固定见 `pyproject.toml` + `依赖清单.md`（`uv sync --extra gpu`；`elefant` 由 `--official-repo` 指路）。
- **GPU 数据存放**：长期文件放 `/root/workspace`（他处随时丢失）。
- **git push**：走本机 Clash 代理 `127.0.0.1:7890`，否则直连被墙。**注意：代理仅用于 git push 等轻量请求；大批量数据下载（huggingface/curl 下 batch、权重）应直连 CDN，代理反而慢且易断**（第 6 天实测：直连 us.aws.cdn.hf.co ≈13.7MB/s，走代理 ≈0.6MB/s 且 43GB 反复断）。

## 四、新对话开工流程（新对话第一步照做）

1. 读本文件（已自动加载），掌握总则 / 索引 / 口径速览。
2. 读 `项目备忘.md`：拿到硬约束、已定口径、**第 6 天计划（拆两块 + 执行顺序）**、待决问题。
3. 改脚本 / 数据前，按需读 `接口约定.md` / `数据模型.md`；跑主路径前读 `演示说明.md`。
4. 开工前先向用户**复述当前进度 + 下一步计划**，确认无误再动手。

> **当前进度（第 6 天收尾，08-25 更新）**：主路径已贯通（88.0% / 0.756），harness 已建。块 1 依赖固定完成（`pyproject.toml` + `依赖清单.md` 落地；服务器 `uv sync --extra gpu` 自测通过）。块 2 微调全链跑通但**未达 +8pp**：COD 2h 微调两轮退化（88%→81.5%/83%）→ 换 GTA 2h 微调（基线 69.5%，best step400 +1.5pp 在噪声内，700 步 66%）。**结论：2h 单游戏子集微调 150M 不升反降（灾难性遗忘 vs 欠拟合两难），如实记录为合法发现**。报告06 / 开发日志 / 项目备忘已同步。接下来：git 提交推送；第 7–8 天围绕该结论换方法（冻图像 tokenizer / 学习率调度 / 更细 LR 搜索）或如实归档。
