# 项目备忘（课题六 · 行动克隆游戏智能体）

> 仓库中的短文档，记录相对稳定的约定。每次下达新任务前先读本备忘，不靠「昨天口头说过」。过时条目删除或标为「失效」。

## 技术栈

- 语言/框架：Python + PyTorch（依赖以官方 `requirements.txt` 为准）
- 环境：uv + Python（与官方 README 一致，弃 pip/conda）
- 模型：150M（官方入门默认，弃 300M / 600M / 1200M）
- 算力：学校 NVIDIA 服务器（本机无 N 卡，CPU 兜底）
- 工具：CodeBuddy、Git、JupyterLab、Windows 录屏（Win+G）

## 主路径接口索引

| 脚本 | 职责 | 约定文件 |
|---|---|---|
| `preprocess.py` | 原始数据 → 200 帧清洗样本 + 统计表 | 接口约定.md |
| `infer.py` | 150M 权重 + 样本 → 预测动作 | 接口约定.md |
| `evaluate.py` | 预测 + 标注 → 按键准确率 / 鼠标相关系数 | 接口约定.md |
| `train.py`（扩展） | 微调（数据来源待确认，暂不落地） | 接口约定.md |

## 临时假实现边界

第 4-5 天用 `# TODO(假实现)` 标明的占位实现，返回固定假数据；真实实现落地即删标记；归档时带标记者不得当真实实现。

## 待决问题列表

1. 微调「1 款游戏 ≤2h 数据」来源 vs「不得下载 full-data 全库」冲突 → 待问老师。
2. 200 帧测试集用 toy 还是某款游戏 → 待定。

## 硬约束（全程）

- 不得下载 `full-data` 全库（指导书 4.6）。
- 不写密钥、仓库地址、录屏外链。
- 仓库 <10MB（`.gitignore` 挡数据 / 权重 / 老师课件）。
