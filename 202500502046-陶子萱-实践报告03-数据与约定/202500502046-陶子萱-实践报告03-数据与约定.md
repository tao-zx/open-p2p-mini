# 实践报告03 · 课题六：行动克隆游戏智能体（第 3 天）

## 基本信息

- 课题编号与名称：课题六 —— 行动克隆游戏智能体（腾讯 IEG 课题）
- 成员：陶子萱（独立完成）

## 今日目标

把主路径上“要持久化哪些数据”和“步骤之间怎么交接”写成书面约定，落地仓库。他人读完本报告应能了解：关键对象有哪些、成功与失败各怎么表现、哪些环节暂用假实现、计划哪一天换成真实实现。产物：数据模型、接口约定、临时假实现边界、项目备忘、当天计划。

## 主路径上的数据或对象

主路径（第 1 天定、第 5 天演示）：

```
toy 原始数据 → 预处理 → 清洗样本(200 帧) → 推理(150M) → 预测动作 → 评测 → 指标(按键准确率/鼠标相关系数) → 演示
```

按要求，实验类课题按“数据切片、指标记录”对象写。主路径上持久化**三个**对象，各有明确用途：原始数据（预处理的输入）、样本切片（推理输入 + 评测真值）、指标记录（验收判据）。（已对照需求删掉验收用不到的 `video_id`、`env`）。200 帧测试集与训练/微调数据隔离；鼠标指标用相关系数（也可用位移误差，二选一）。

### 实体 0：原始数据（输入）

用途：preprocess 的唯一输入——从中切出游戏帧、读出逐帧人类动作标注。

| 字段 | 含义 | 出处 |
|---|---|---|
| video_file（.mp4） | 游戏画面视频，192×192 | 官方数据格式 |
| annotation_file（.proto） | 逐帧动作标注（`VideoAnnotation`） | 官方 proto |

### 实体 1：样本切片 Sample（含逐帧动作 FrameAction）

用途：主路径中枢——喂给 infer 做推理输入，同时给 evaluate 当“真值基准”算指标。

对应“清洗样本”——从原始视频切出的帧 + 逐帧人类动作标注。**1 条 = 1 帧画面 + 1 条动作标注，200 条 = 200 帧测试集。**

| 字段 | 类型 | 含义 | 出处 |
|---|---|---|---|
| frame | 图片（192×192 彩色） | 一帧游戏画面 | 官方 `frame_height=192`、`frame_width=192` |
| action | FrameAction | 这一帧的人类动作标注 | 官方 `frame_annotations` |

**逐帧动作 FrameAction**（评测直接用它）：

| 字段 | 类型 | 含义 | 出处 |
|---|---|---|---|
| keys | 列表 | 该帧按下的键盘键码 | 官方 `user_action.keyboard.keys` |
| mouse_buttons | 列表 | 该帧按下的鼠标键码 | 官方 `user_action.mouse.buttons_down` |
| mouse_delta_x | 整数 | 鼠标水平位移 | 官方 `user_action.mouse.mouse_delta_px.x` |
| mouse_delta_y | 整数 | 鼠标垂直位移 | 官方 `user_action.mouse.mouse_delta_px.y` |
| is_user_action | 布尔 | 是否人类动作（区分系统动作） | 官方 `user_action.is_known` |

说明：① `mouse_buttons` 是动作本体（模型会预测它），但两个验收指标都不算它——按键准确率看 `keys`，鼠标相关系数只看 `mouse_delta_x/y`；② 评测只取 `is_user_action = 真` 的人类动作，忽略系统动作（如镜头自动旋转），避免误判。

### 实体 2：指标记录 MetricRecord

用途：验收判据——第 5 天演示与结课报告都从这里取值。

| 字段 | 类型 | 含义 | 验收阈值 |
|---|---|---|---|
| key_accuracy | 小数 | 按键准确率 | ≥55% |
| mouse_correlation | 小数 | 鼠标相关系数（位移相关） | ≥0.5 |
| n_samples | 整数 | 评测样本数 | 200 |
| action_distribution | 表 | 动作分布统计 | 供统计表 |

> 口径说明：`n_samples = 200`（= 200 帧 = 200 条）是评测集大小；演示用“同一测试片段”（模型输出 vs 人类标注对比，必做4），与 `n_samples` 无关。

字段一致性：三处（preprocess 产出 / infer 读入 / evaluate 读入）字段名统一为英文名（`keys` / `mouse_buttons` / `mouse_delta_x` / `mouse_delta_y`），与官方代码同名，无需另做字段对照表。

## 调用约定

接口约定 = 命令 + 参数 + 成功/失败返回，统一退出码 0=成功 / 2=参数错误 / 3=数据错误。主路径最关键一步 **`infer.py`（推理）** 作完整范例：

**`infer.py` 成功一例：**

- 输入要点：`--weights` 指向 150M 权重；`--samples` 指向 `samples.json`
- 输出要点：`predictions.json`，每帧 4 个动作字段

```
$ python infer.py --weights ckpt/150m.pt --samples out/samples.json --out pred
[OK] 推理 200 帧完成
[OK] 写出 pred/predictions.json
```

**`infer.py` 失败一例（权重缺失）：**

```
$ python infer.py --weights no_ckpt.pt --samples out/samples.json --out pred
[ERROR] 权重文件不存在: no_ckpt.pt（退出码 3）
```

其余两步同格式，各举一例（失败例覆盖“参数错误”与“缺文件”两类）：

**`preprocess.py` 成功一例：**

```
$ python preprocess.py --data_dir toy-examples --out_dir out --n_frames 200
[OK] 处理 200 帧 → 200 条样本，写出 out/samples.json、stats.csv
```

**`preprocess.py` 失败一例（参数错误）：**

```
$ python preprocess.py --data_dir toy-examples --out_dir out --n_frames -5
[ERROR] --n_frames 必须是正整数，收到 -5（退出码 2）
```

**`evaluate.py` 成功一例：**

```
$ python evaluate.py --pred pred/predictions.json --label out/samples.json
[OK] 按键准确率 = 58.0%（阈值 55%）
[OK] 鼠标相关系数 = 0.52（阈值 0.5），写出 metrics.json
```

**`evaluate.py` 失败一例（帧数不一致）：**

```
$ python evaluate.py --pred pred/predictions.json --label out/samples.json
[ERROR] 预测 199 帧 vs 标注 200 帧，帧数不一致（退出码 3）
```

`train.py`（微调）留接口、暂不落地；数据来源已确认：从 full-data 下载“1 款游戏、2 小时数据”子集（不下载全库）；微调指标 = 按键 +8pp 或 鼠标 +0.08。

各脚本完整的命令、参数、退出码与全部失败例，见仓库内 `接口约定.md`；本报告只列主路径上的成功例与失败例。

## 临时假实现

第 3 天只定约定、不写真实逻辑。第 4-5 天先用“临时假实现”占位，顶替三个尚未就绪的外部依赖，文件顶部 + 每个函数用 `# TODO(假实现)` 标明：

| 脚本 | 顶替的外部依赖 | 假实现行为 | 第几天替换 |
|---|---|---|---|
| `preprocess.py` | toy 数据（尚未下载） | 返回固定 200 条空样本（动作全 0） | 第 4 天 |
| `infer.py` | 150M 权重（尚未下载） | 返回全 0 动作 | 第 5 天（演示前） |
| `evaluate.py` | 前两步的真实产物 | 返回 0% / 0.0 | 第 5 天（演示前） |

约定：替换后删除 `# TODO(假实现)` 标记；归档时若还带此标记，一律不得当作已接真实实现。

## 项目备忘要点

已在仓库根目录建好“项目备忘.md”，记录相对稳定的约定，每次下任务前先读。要点：

- **技术栈**：Python + PyTorch；uv；模型 150M；自研评测脚本；学校 NVIDIA 服务器。
- **约定索引**：preprocess / infer / evaluate 三脚本 + train（扩展），详见接口约定.md。
- **待决问题**：已定——200 帧用 toy、微调用 full-data 子集（1 款游戏 2 小时，指标按键 +8pp 或 鼠标 +0.08）；待定——微调哪款游戏与下载方式，第 6 天定。

## 今日完成与自检

已确定：数据模型（三类对象）、调用约定（成功例 + 失败例）、临时假实现（含替换日）、项目备忘、当天计划。

自检（对照评分点）：① 主路径——三对象足以支撑“预处理→推理→评测→演示”；② 约定——infer 兼有成功例与失败例；③ 假实现——三脚本标 `# TODO(假实现)` 并写替换日，不会被误当已完成。

## 问题、计划与 AI 沟通

**不成功的沟通：** 助手第一版把官方源码字段全列了出来（含验收用不到的 `video_id`、`env`），字段表偏大、偏离主路径。我按“只列支撑主路径的关键实体”重提要求，逐字段对照需求删冗余，才收敛成三个对象。

**比较有效的沟通：** 我把老师第 4 节要求原文贴给助手——就主路径上最关键的一步，至少写明成功一例（输入要点与输出要点）与失败一例。照这条审查后，成功例带上了输入/输出要点、失败例写清了返回什么和退出码，得到能直接照着实现的调用约定，避免只有成功一条路。

**明日安排：** 先做环境准备——clone 仓库、装依赖、下载 150M 权重与 toy 数据，再按本报告调用约定搭三个脚本的临时假实现，跑通 demo。
